{{ config(tags=['intermediate', 'health', 'garmin']) }}

select
  {{ health_intermediate_common_columns('stg') }},
  stg.payload_id,
  stg.profile_id,
  stg.connection_request_id,
  stg.garmin_guid,
  stg.profile_external_id,
  stg.display_name,
  stg.full_name,
  stg.user_name,
  stg.user_profile_full_name,
  stg.profile_image_type,
  stg.profile_image_uuid,
  stg.profile_image_url_large,
  stg.profile_image_url_medium,
  stg.profile_image_url_small,
  stg.location,
  stg.facebook_url,
  stg.twitter_url,
  stg.personal_website,
  stg.motivation,
  stg.bio,
  stg.primary_activity,
  stg.other_primary_activity,
  stg.running_training_speed,
  stg.cycling_training_speed,
  stg.swimming_training_speed,
  stg.cycling_classification,
  stg.cycling_max_avg_power,
  stg.other_activity,
  stg.other_motivation,
  stg.profile_visibility,
  stg.activity_start_visibility,
  stg.activity_map_visibility,
  stg.course_visibility,
  stg.activity_heart_rate_visibility,
  stg.activity_power_visibility,
  stg.badge_visibility,
  stg.show_age,
  stg.show_weight,
  stg.show_height,
  stg.show_weight_class,
  stg.show_age_range,
  stg.show_gender,
  stg.show_activity_class,
  stg.show_vo2_max,
  stg.show_personal_records,
  stg.show_last_12_months,
  stg.show_lifetime_totals,
  stg.show_upcoming_events,
  stg.show_recent_favorites,
  stg.show_recent_device,
  stg.show_recent_gear,
  stg.show_badges,
  stg.name_approved,
  stg.make_golf_scorecards_private,
  stg.allow_golf_live_scoring,
  stg.allow_golf_scoring_by_connections,
  stg.user_level,
  stg.user_point,
  stg.level_update_at,
  stg.level_is_viewed,
  stg.level_point_threshold,
  stg.user_point_offset,
  stg.user_pro
from {{ ref('stg_garmin_profile_snapshot') }} as stg
